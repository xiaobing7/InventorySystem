syntax = "proto3";

import "book.proto";

message InventoryItem{
  int64 inventory_number = 1;
  oneof books{
    Book book= 2;
  }
  enum Status{
  AVAILABLE = 0;
  TAKEN = 1;
  }
  Status status = 3;
}
